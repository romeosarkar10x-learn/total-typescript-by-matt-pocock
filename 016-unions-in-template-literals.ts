type BreadType = "rye" | "brown" | "white";
type Filling = "cheese" | "ham" | "salami";

type Sandwich = `${Filling} sandwich with ${BreadType}`;

// const object = { type: true, value: "false" };

// type Test = `${ any ? "gadha" : "intelligent"}`;
